@{
    ViewBag.Title = "AvaliableOperations";
}

<div class="jumbotron">
    <h1>Операции</h1>
</div>

<div class="row">

    @foreach (var operation in ViewBag.Operation as IEnumerable<DomainModels.Models.Operation>)
    {
        <div class="col-md-4">                   
            <p>@operation.Name</p>
            <p>@operation.FullName</p>
            <p>@operation.Id</p>
        </div>
    }

</div>
